/* Holds TransactionList class, which represents a list of transactions for a given account. Also determines account balance. */

class TransactionList {
  constructor(transactions) {
    this.transactions = transactions;
  }
}