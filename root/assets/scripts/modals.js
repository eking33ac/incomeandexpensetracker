"use strict";

// This file is fairly likely to be merged with others or separated out itself. Currently exists to create and test modals, as well as hold assignment client-side form validation instructions for Phase 2.

/* instructions:

All forms in your application must validate input before submission. Validation must be implemented
in JavaScript, not solely through HTML required attributes (though you may use both).
At minimum, your create/edit form must validate:
• Required fields - display an error if the user attempts to submit with an empty required field
• Format constraints - at least one field must enforce a format rule (email format, minimum
length, numeric range, date in the future, etc)
• Error display - errors must appear as visible text adjacent to the relevant input field, not only
as browser alerts or console messages
• Error clearing - error messages must disappear when the user corrects the input

*/
